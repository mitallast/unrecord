# Unrecord

Simple utility to test Mac OS specific core audio device loopback degradation

## How to build

```shell
git clone https://github.com/mitallast/unrecord.git unrecord
cd unrecord
cargo build --release
./target/release/unrecord
```
